#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* strlen - scale lenght of a string
*
* @s: string
*/
int _strlen(char *s)
{
  int len;
  len = _strlen(s);

printf("%d\n", len);



return (0);
}
