#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * _isupper - print if the input uppercase
 * @c: the int that will be checked
 * Return: 0
*/
int _isupper(int c)
{

  if (isupper(c))
    printf("%c is %d", c, isupper(c));
  else
    printf("%c is %d", c, isupper(c));
  return (0);

}
